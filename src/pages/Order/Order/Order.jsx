import { Helmet } from "react-helmet";

const Order = () => {
  return (
    <div>
      <Helmet>
        <title>Bistro || Food Order</title>
      </Helmet>
    </div>
  );
};

export default Order;
